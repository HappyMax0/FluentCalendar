<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CalendarWinUI3.Views.HolidaysPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CalendarWinUI3.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:CalendarWinUI3.Models" xmlns:converter="using:CalendarWinUI3.Views.Converter"
    mc:Ignorable="d">
    <Page.Resources>
        <converter:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>
    </Page.Resources>
    <Grid Margin="0,24,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DatePicker x:Name="datePicker" Header="Pick a date" Margin="24,0" DateChanged="datePicker_DateChanged"/>

        <TreeView Grid.Row="1" x:Name="holidayTreeView" Margin="12,12,0,0">
            <TreeView.ItemTemplate>
                <DataTemplate x:DataType="models:Holiday">
                    <StackPanel >
                        <TextBlock Text="{x:Bind Name}"/>
                        <TextBlock Text="放假"/>
                        <TreeView ItemsSource="{x:Bind Holidays}"/>
                        <TextBlock Text="补班" Visibility="{x:Bind WorkdayCount, Converter={StaticResource IntToVisibilityConverter}}"/>
                        <TreeView ItemsSource="{x:Bind Workdays}"/>
                    </StackPanel>
                </DataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</Page>
