<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CalendarWinUI3.Views.OldAlmanacPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CalendarWinUI3.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="48">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border
    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
    CornerRadius="8"
    Padding="16"
    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
    BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="{x:Bind ViewModel.SolarDate}" FontSize="20"/>
                    <TextBlock Text="{x:Bind ViewModel.SixtyCycleDate}"/>
                    <TextBlock Text="{x:Bind ViewModel.LunarDate}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1"
Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
CornerRadius="8"
Padding="16"
BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
BorderThickness="1">
            <StackPanel Grid.Column="1" HorizontalAlignment="Left" Margin="12">
                <TextBlock x:Uid="MonthPage_Recommended" Margin="0,10,0,5" FontSize="22" IsColorFontEnabled="True" Foreground="Green" TextWrapping="WrapWholeWords"/>
                <TextBlock Text="{x:Bind ViewModel.Recommends}"/>
                <TextBlock x:Uid="MonthPage_Prohibited" Margin="0,10,0,5" FontSize="22" IsColorFontEnabled="True" Foreground="Red" TextWrapping="WrapWholeWords"/>
                <TextBlock Text="{x:Bind ViewModel.Avoids}"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2"
Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
CornerRadius="8"
Padding="16"
BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
BorderThickness="1">
            <StackPanel HorizontalAlignment="Left" Margin="12">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Uid="GoodGods" Margin="10" FontSize="18" TextWrapping="WrapWholeWords" Foreground="Red"/>
                    <TextBlock Text="{x:Bind ViewModel.GoodGods}" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Uid="BadGods" Margin="10" FontSize="18" TextWrapping="WrapWholeWords" Foreground="Red"/>
                    <TextBlock Text="{x:Bind ViewModel.BadGods}" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Uid="OldAlmanacPage_PengZu" Margin="10" FontSize="18" TextWrapping="WrapWholeWords" Foreground="Red"/>
                    <TextBlock Text="{x:Bind ViewModel.PengZu}" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Border>

    </Grid>
</Page>
