<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CalendarWinUI3.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CalendarWinUI3"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:CalendarWinUI3.Models"
    mc:Ignorable="d">

    <Grid>
        <Grid x:Name="AppTitleBar"
              Height="32" Margin="48,0,0,0"
              VerticalAlignment="Top"
              Canvas.ZIndex="1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                <Image Source="Assets/StoreLogo.png" 
                       Width="16" Height="16" 
                       Stretch="Uniform"/>
                <TextBlock Text="FluentCalendar" 
                           VerticalAlignment="Center" 
                           Margin="12,0,0,0" 
                           Style="{StaticResource CaptionTextBlockStyle}"/>
            </StackPanel>
        </Grid>
        <NavigationView x:Name="navigationView"
                 ItemInvoked="navigationView_ItemInvoked"
                 SelectionChanged="navigationView_SelectionChanged"
                 BackRequested="navigationView_BackRequested"
                 IsBackButtonVisible="Visible">
            <NavigationView.AutoSuggestBox>
                <AutoSuggestBox/>
            </NavigationView.AutoSuggestBox>
            <NavigationView.MenuItems>
                <NavigationViewItem x:Uid="NavigationViewItem_Calendar"  Icon="Calendar" Tag="Calendar" IsSelected="True">
                    <TextBlock x:Uid="MainWindow_CalendarItem"/>
                </NavigationViewItem>
                <NavigationViewItemSeparator/>
                <NavigationViewItem Icon="CalendarDay" Tag="OldAlmanac">
                    <TextBlock x:Uid="MainWindow_OldAlmanacItem"/>
                </NavigationViewItem>
                <NavigationViewItem Icon="Clock" Tag="Holidays">
                    <TextBlock x:Uid="MainWindow_HolidayItem"/>
                </NavigationViewItem>
                <NavigationViewItem Icon="TouchPointer" Tag="Toolbox">
                    <TextBlock x:Uid="MainWindow_ToolboxItem" />
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Frame x:Name="contentFrame"/>
        </NavigationView>
    </Grid>
</Window>
    